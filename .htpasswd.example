# Copy to .htpasswd, then add real credentials.
# Never commit real passwords.
#
# htpasswd:
#   htpasswd -c .htpasswd your_username
#
# openssl alternative:
#   echo "username:$(openssl passwd -apr1)" >> .htpasswd
#
# Enable with: PASSWORD_GATE_ENABLED=true
