some quick steps for myself for testing

1. use the Dev overlay (bottom-right corner) to create or log in as a user
2. use Dev Actions (top-right) -> Create seeded test bracket
3. start noms (or continue with the seeded bracket)

tips:
- add `?flushCache` to flush the cache...
